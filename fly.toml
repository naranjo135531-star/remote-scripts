# fly.toml — abb

app = 'abb'
primary_region = 'gru'

[build]

[env]
  PORT = '8000'
  ENVIRONMENT = 'production'
  ENABLED_ADMIN_PANEL = 'true'

[deploy]
  release_command = 'alembic upgrade head'

[http_service]
  internal_port = 8000
  force_https = true
  auto_stop_machines = 'stop'
  auto_start_machines = true
  min_machines_running = 0
  processes = ['app']

[[vm]]
  memory = '512mb'
  cpu_kind = 'shared'
  cpus = 1
